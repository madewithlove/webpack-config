import React, {Component, PropTypes} from 'react';

export default class SomeComponent extends Component {
    static propTypes = {};

    render() {
        return (
            <div>Foobar</div>
        );
    }
}
